
<div class="loginform">
    <div class="row">
        <center><h1 class="page-header">Listings</h1></center>

        <% @listings.each do |listing| %>

            <% if listing.attachments.first  %>
        
                <div class ="listingtiles col-md-3 img-portfolio">
                    <%= link_to listing do %> 
                        <%= image_tag listing.attachments.first.image.url(:thumb) %>
                    <% end %>
                    <div class="inner">
                        <hr>
                        
                        <h3><%= link_to listing.title, listing %></h3> 

                
                        <%= listing.description.truncate(75) %>
                    </div>
                    <div class="viewbutton">
                    <%= link_to 'View', listing.title, class: 'btn btn-primary' %>
                    </div>
                </div>

            <% end %>
        
        <% end %>
           <% if user_signed_in? %>
<p><%= link_to 'Add a Listing', new_listing_path, class: 'btn btn-primary' %></p>
<% end %>
    </div>
</div>
<div>

</div>